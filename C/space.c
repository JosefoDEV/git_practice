#include <stdio.h>

// Task:
// Little Mac is an interplanetary space boxer, who is trying
// to win championship belts for various weight categories on
// other planets within the solar system.
//
// Write a space.c program that helps him keep track of his target
// weight by: It should ask him what his earth weight is.
// Ask him to enter a number for the planet he wants to fith on.
// It should then compute his weigth on the destination planet.

// Here is table of conversion:
// ||= Planet =||= Relative Gravity =|| 
// || Mercury  || 0.38
// || Venus    || 0.91
// || Mars     || 0.38
// || Jupiter  || 2.34
// || Saturn   || 1.06
// || Uranus   || 0.92
// || Neptune  || 1.19

int main() {










}